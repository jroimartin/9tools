#!/bin/sh

# chmod file and run B, then wait for the file to change.
# the notion of a file changing is a little weak.

sudo chmod o+w "$@"

stat=`ls -l $1`
B "$@"
echo editing "$@"
while sleep 1
do
	nstat=`ls -l $1`
	if [ "x$stat" != "x$nstat" ]
	then
		break
	fi
done

sudo chmod o-w "$@"
